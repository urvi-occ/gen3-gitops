apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gen3-chordstaging
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: test
  sources:
    - path: helm/gen3
      repoURL: https://github.com/uc-cdis/gen3-helm
      targetRevision: "master"
      helm:
        releaseName: chordstagingargo
        valueFiles:
          - $values/staging.chordshealth.org/values/etl.yaml
          - $values/staging.chordshealth.org/values/values.yaml
          - $values/staging.chordshealth.org/values/fence.yaml
          - $values/staging.chordshealth.org/values/portal.yaml
          - $values/staging.chordshealth.org/values/guppy.yaml
    - repoURL: 'https://github.com/uc-cdis/gen3-gitops.git'
      targetRevision: master
      ref: values
  destination:
    server: "https://kubernetes.default.svc"
    namespace: chordstaginghelm
  syncPolicy:
    syncOptions:
    - CreateNamespace=false
    automated:
      selfHeal: true
