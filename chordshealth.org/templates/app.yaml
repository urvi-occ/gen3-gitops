apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gen3-chordsprod
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - path: helm/gen3
      repoURL: https://github.com/uc-cdis/gen3-helm
      targetRevision: "master"
      helm:
        releaseName: chordsprodargo
        valueFiles:
          - $values/chordshealth.org/values/etl.yaml
          - $values/chordshealth.org/values/values.yaml
          - $values/chordshealth.org/values/fence.yaml
          - $values/chordshealth.org/values/portal.yaml
          - $values/chordshealth.org/values/guppy.yaml
    - repoURL: 'https://github.com/uc-cdis/gen3-gitops.git'
      targetRevision: master
      ref: values
  destination:
    server: "https://kubernetes.default.svc"
    namespace: default
  syncPolicy:
    syncOptions:
    - CreateNamespace=false
    automated:
      selfHeal: true
