apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gen3
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - path: helm/gen3
      repoURL: https://github.com/uc-cdis/gen3-helm
      targetRevision: master
      helm:
        releaseName: oadc
        valueFiles:
          - $values/gen3.datacommons.org/values/values.yaml
          - $values/gen3.datacommons.org/values/fence.yaml
          - $values/gen3.datacommons.org/values/portal.yaml
          - $values/gen3.datacommons.org/values/guppy.yaml
    - repoURL: 'https://github.com/uc-cdis/gen3-gitops.git'
      targetRevision: master
      ref: values
  destination:
    server: "https://kubernetes.default.svc"
    namespace: oadchelm
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    automated:
      selfHeal: true